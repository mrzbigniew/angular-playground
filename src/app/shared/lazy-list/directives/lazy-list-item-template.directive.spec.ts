import { LazyListItemTemplateDirective } from './lazy-list-item-template.directive';

describe('LazyListItemTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new LazyListItemTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
