import { OnBoardingSliderPaginationItemTemplateDirective } from './on-boarding-slider-pagination-item-template.directive';

describe('OnBoardingSliderPaginationItemTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new OnBoardingSliderPaginationItemTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
